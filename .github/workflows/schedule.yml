name: Scheduling my first

on:
  schedule:
    - cron: "0 * * * * "

jobs: 
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Schedule workflow executed"
